# Set all elements of a `difftime` object to the same, largest meaningful unit

Set all elements of a `difftime` object to the same, largest meaningful
unit

## Usage

``` r
reformat_difftime(dt)
```

## Arguments

- dt:

  a `difftime` object (potentially a vector of `difftime`s)
