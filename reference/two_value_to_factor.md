# Convert two-value vectors to factors

Convert two-value vectors to factors

## Usage

``` r
two_value_to_factor(x)
```

## Arguments

- x:

  a vector

## Value

the vector converted to a factor if it has exactly two unique
(non-missing) values and is not already a factor; otherwise the input
