# Converts a `difftime` object to a `data.frame`

Converts a `difftime` object to a `data.frame`

## Usage

``` r
difftime_df(dt)
```

## Arguments

- dt:

  `difftime` object (vector of `difftime` objects)
