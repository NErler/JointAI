# Check for unnecessary grouping levels

Used within [`get_groups()`](get_groups.md).

## Usage

``` r
check_redundant_lvls(groups, nrow_data)
```

## Arguments

- groups:

  a list of integer "id" variables

## Value

NULL; throws an error if unnecessary grouping levels are found
