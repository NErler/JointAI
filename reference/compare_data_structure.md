# Compare the structure of two data.frames

Compare the structure of two data.frames

## Usage

``` r
compare_data_structure(data1, data2)
```

## Arguments

- data1:

  a `data.frame`

- data2:

  a `data.frame`

## Value

nothing, but prints messages if the class of any variable changed or if
the levels of any factor variable changed
