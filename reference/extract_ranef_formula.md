# Extract random effects formula from lme4-type formula

Extract random effects formula from lme4-type formula

## Usage

``` r
extract_ranef_formula(formula)
```

## Arguments

- formula:

  a `formula` object (typically in lme4-style)

## Value

a one-sided `formula` object (or `NULL` if there are no random effects)
